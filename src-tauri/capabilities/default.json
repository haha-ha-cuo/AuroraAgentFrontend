{
  "identifier": "default",
  "description": "主窗口默认能力：事件订阅 + 窗口基本权限（最小化）。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "dialog:allow-open"
  ]
}
