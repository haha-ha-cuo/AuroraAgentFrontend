# Python 后端目录。默认使用与 AuroraAgentFrontend 同级的 AuroraAgentBackend。
# AURORA_ROOT=/absolute/path/to/AuroraAgentBackend

# 浏览器联调默认连接本地 8765 端口；显式设置后不自动启动本地后端。
# 手动启动：在后端目录运行 uv run aurora runtime --port 8765。
# VITE_RUNTIME_WS=ws://127.0.0.1:8765/ws
